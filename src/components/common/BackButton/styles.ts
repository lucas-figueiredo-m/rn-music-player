import { StyleSheet } from 'react-native'
import { Colors } from 'styles';


export const styles = StyleSheet.create({
  root: {
    width: 40,
    height: 40,
    backgroundColor: Colors.FullShadeTranslucent,
    borderRadius: 25,
    alignItems: 'center',
    justifyContent: 'center',
    marginLeft: '5%'
  }
})